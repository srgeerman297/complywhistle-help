let qnaData=[];let recognition=null;let isListening=false;let micButton=null;let userInput=null;function normalize(t){return t.toLowerCase().replace(/[^\w\s]/g,"").replace(/\s+/g," ").trim()}function scoreMatch(u,e){const q=normalize(u);let s=0;for(const question of e.questions){const n=normalize(question);if(q===n)s+=100;if(q.includes(n))s+=40;if(n.includes(q))s+=25;const w=q.split(" "),sw=n.split(" ");s+=w.filter(word=>sw.includes(word)).length*4}for(const keyword of e.keywords){if(q.includes(normalize(keyword)))s+=10}return s}function findBestAnswer(question){let best=null,bestScore=0;for(const entry of qnaData){const score=scoreMatch(question,entry);if(score>bestScore){bestScore=score;best=entry}}if(!best||bestScore<20)return"I could not find a clear answer in the current ComplyWhistle help content yet. Please contact your administrator or AXIOMA for assistance.";return best.answer}function addMessage(text,sender){const chatBox=document.getElementById("chat-box");if(!chatBox)return;const message=document.createElement("div");message.className=`message ${sender}`;message.textContent=text;chatBox.appendChild(message);chatBox.scrollTop=chatBox.scrollHeight}function updateAssistantStatus(message){const status=document.getElementById("assistant-status");if(status)status.textContent=message}function setMicState(listening){isListening=listening;if(!micButton)return;micButton.classList.toggle("listening",listening);micButton.setAttribute("aria-pressed",String(listening));micButton.setAttribute("title",listening?"Stop voice input":"Use voice input");micButton.textContent=listening?"Stop":"Mic"}async function loadQnA(){try{const response=await fetch("data/qna.json",{cache:"no-store"});if(!response.ok)throw new Error("Could not load Q&A data.");qnaData=await response.json()}catch(error){addMessage("The help content could not be loaded right now. Please refresh the page and try again.","bot");console.error(error)}}function askQuestion(question){const cleaned=question.trim();if(!cleaned)return;addMessage(cleaned,"user");addMessage(findBestAnswer(cleaned),"bot")}async function requestMicPermission(){if(!navigator.mediaDevices||!navigator.mediaDevices.getUserMedia)return true;const stream=await navigator.mediaDevices.getUserMedia({audio:true});stream.getTracks().forEach(track=>track.stop());return true}function initializeVoiceInput(){micButton=document.getElementById("mic-button");userInput=document.getElementById("user-input");if(!micButton||!userInput)return;const SpeechRecognition=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SpeechRecognition){micButton.disabled=true;updateAssistantStatus("Voice input is not supported in this browser. You can still type your question.");return}recognition=new SpeechRecognition();recognition.lang="en-US";recognition.interimResults=true;recognition.continuous=false;recognition.maxAlternatives=1;let finalTranscript="";let shouldAutoSubmit=false;recognition.onstart=()=>{finalTranscript="";shouldAutoSubmit=false;setMicState(true);updateAssistantStatus("Listening... Speak your question now.")};recognition.onresult=(event)=>{let interim="";for(let i=event.resultIndex;i<event.results.length;i+=1){const transcript=event.results[i][0].transcript.trim();if(event.results[i].isFinal){finalTranscript=`${finalTranscript} ${transcript}`.trim();shouldAutoSubmit=true}else{interim=`${interim} ${transcript}`.trim()}}const combined=`${finalTranscript} ${interim}`.trim();if(combined)userInput.value=combined};recognition.onerror=(event)=>{setMicState(false);shouldAutoSubmit=false;let message="Voice input could not be started. Please try again.";switch(event.error){case"not-allowed":case"service-not-allowed":message="Microphone access was blocked. Please allow microphone access and try again.";break;case"no-speech":message="No speech was detected. Please click Mic and try again.";break;case"audio-capture":message="No microphone was detected. Please check your microphone and try again.";break;case"network":message="Voice input hit a network issue. Please try again.";break;default:message=`Voice input error: ${event.error}. Please try again.`}updateAssistantStatus(message)};recognition.onend=()=>{const transcript=userInput.value.trim();setMicState(false);if(shouldAutoSubmit&&transcript){updateAssistantStatus("Voice input captured. Sending your question...");askQuestion(transcript);userInput.value="";updateAssistantStatus("Voice input ready. Click Mic to speak your question.");return}if(!userInput.value.trim())updateAssistantStatus("Voice input ready. Click Mic to speak your question.");else updateAssistantStatus("Voice captured. You can edit the text or click Send.")};micButton.addEventListener("click",async()=>{if(!recognition)return;if(isListening){recognition.stop();return}try{updateAssistantStatus("Checking microphone permission...");await requestMicPermission();userInput.focus();recognition.start()}catch(error){console.error(error);setMicState(false);updateAssistantStatus("Microphone access was denied or unavailable. Please allow microphone access in Chrome and try again.")}});updateAssistantStatus("Voice input ready. Click Mic to speak your question.")}document.addEventListener("DOMContentLoaded",async()=>{await loadQnA();initializeVoiceInput();const chatForm=document.getElementById("chat-form");userInput=document.getElementById("user-input");document.querySelectorAll(".suggestion-chip").forEach(button=>button.addEventListener("click",()=>{const question=button.getAttribute("data-question")||"";askQuestion(question);if(userInput)userInput.focus()}));if(chatForm&&userInput){chatForm.addEventListener("submit",event=>{event.preventDefault();askQuestion(userInput.value);userInput.value="";userInput.focus();updateAssistantStatus("Voice input ready. Click Mic to speak your question.")})}});